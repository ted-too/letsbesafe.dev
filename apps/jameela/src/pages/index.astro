---
import SearchForm from "../components/search-form";
import Layout from "../Layout.astro";
import getTheme from "ui/utils/getTheme";

const theme = getTheme(Astro);
---

<Layout theme={theme}>
  <SearchForm firstSearch={true} client:visible />
</Layout>
